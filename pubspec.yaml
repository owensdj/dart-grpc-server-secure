name: dart_grpc_server
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.0 <3.0.0'

dependencies:
  dbcrypt:
  grpc:
  jaguar_jwt:
  protobuf:


dev_dependencies:
  pedantic: ^1.10.0
  test: ^1.16.0
